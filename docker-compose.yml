services:
  mysql:
    image: mysql:8.0
    container_name: goodsrush-mysql
    environment:
      MYSQL_ROOT_PASSWORD: 1234
      MYSQL_DATABASE: goodsrush
      TZ: Asia/Seoul
    ports:
      - "3307:3306"
    volumes:
      - goodsrush-mysql-data:/var/lib/mysql

volumes:
  goodsrush-mysql-data: